services:
  shadowsocks:
    image: shadowsocks/shadowsocks-libev
    container_name: shadowsocks
    ports:
      - "${PORT:-8388}:8388/tcp"
      - "${PORT:-8388}:8388/udp"
    env_file: .env
    restart: unless-stopped
