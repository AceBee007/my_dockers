services:
  shadowsocks:
    image: shadowsocks/shadowsocks-libev
    container_name: shadowsocks
    ports:
      - "${DOCKER_PORT:-8388}:${PORT:-8388}/tcp"
      - "${DOCKER_PORT:-8388}:${PORT:-8388}/udp"
    env_file: .env
    restart: unless-stopped
