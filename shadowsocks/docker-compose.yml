services:
  shadowsocks:
    image: shadowsocks/shadowsocks-libev
    container_name: shadowsocks
    ports:
      - "${HOST_PORT:-8388}:${SERVER_PORT:-8388}/tcp"
      - "${HOST_PORT:-8388}:${SERVER_PORT:-8388}/udp"
    env_file: .env
    restart: unless-stopped
