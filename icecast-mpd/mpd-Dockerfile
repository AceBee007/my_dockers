#
# Dockerfile for mpd
#

FROM alpine:latest
MAINTAINER EasyPi Software Foundation

RUN set -xe \
    && apk add --no-cache mpd mpc ncmpcpp pulseaudio pulseaudio-alsa \
    && setcap -r /usr/bin/mpd

VOLUME /var/lib/mpd

EXPOSE 6600
CMD ["mpd", "--stdout", "--no-daemon"]

